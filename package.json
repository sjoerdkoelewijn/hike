{
  "name": "hike",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env BASE_PATH=/ astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "tina:dev": "cross-env BASE_PATH=/ tinacms dev -c \"astro dev\"",
    "tina:build": "tinacms build --skip-cloud-checks --content=local -c \"astro build\""
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@fontsource/manrope": "^5.3.0",
    "@tinacms/cli": "^2.5.6",
    "astro": "^7.1.3",
    "tinacms": "^3.11.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
